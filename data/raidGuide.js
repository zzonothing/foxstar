// ─────────────────────────────────────────────────────────────────────────────
//  data/raidGuide.js
//  유니온레이드 "육성 가이드" 설정 (공개 정적 파일 · 비밀 없음 · config.js 와 동급)
//
//  ▶ 설계: "라이브 자동추출 + 얇은 운영자 오버레이" (하이브리드)
//    - 중요 캐릭터 목록은 guide.html 이 raid.js(실제 레이드 스쿼드)에서
//      매번 자동 추출한다 → 시즌이 바뀌어도 이 파일을 다시 쓸 필요가 없다.
//    - 이 파일은 자동추출을 "보정"하는 손잡이(must/exclude/tierOverride)와
//      "저육성" 판정 임계값(thresholds)만 담는다.
//  ▶ 운영 방법: 자동 목록이 우리 유니온 감각과 다르면 must/exclude 만 손본다.
// ─────────────────────────────────────────────────────────────────────────────

const RAID_GUIDE = {
  // ── 자동추출 파라미터 ──────────────────────────────────────────
  topN: 24,            // 자동추출 상위 N개를 "중요 캐릭터"로 본다
  recentSeasons: 3,    // 최근 N개 시즌 스쿼드를 가중 집계(최신 시즌일수록 가중치↑)

  // ── 운영자 오버레이(보정) ──────────────────────────────────────
  // 자동 목록에 무조건 포함시킬 캐릭터 이름 (메타엔 떴지만 아직 우리 기록에 덜 잡힌 신캐 등)
  must: [],
  // 자동 목록에서 무조건 제외할 캐릭터 이름 (더 이상 권장하지 않는 캐릭터 등)
  exclude: [],

  // ── "저육성(🟡)" 판정 임계값 ───────────────────────────────────
  //  중요 캐릭터를 "보유"했지만 아래 중 하나라도 해당하면 저육성으로 본다.
  thresholds: {
    fullco: "풀코",   // upgrade 가 이 값이 아니면 코어 부족
    skillSum: 12,     // skill1+skill2+skill3 합이 이 값 미만이면 스킬 부족 (대략 4/4/4)
    levelGap: 60,     // (본인 최고 캐릭터 레벨 − 해당 캐릭터 레벨) 이 값 초과면 레벨 부족
  },

  // "우선 육성 추천"에 강조해서 보여줄 개수
  topPicks: 6,
};
